# Pinned rule set so CI is stable across ruff releases (0.16's built-in
# defaults grew to include stylistic rules like LOG015/BLE001 that this
# codebase uses intentionally).
line-length = 100
exclude = [".bmad", ".venv"]

[lint]
select = ["E4", "E7", "E9", "F", "I"]